<header>
  <span class="logo"><%= link_to("ALK", user_path(id: session.id)) %></span>
  <a href="/users/<%=current_user.id%>">


  <% if current_user.is_admin? %>
    <span class="admin">Admin</span>
    <span class="admin_a"><%= link_to('Users', users_path) %></span>
  <% end %>

</header>
